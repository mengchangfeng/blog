# 博客写作风格

这份文档用于约束博客文章的叙事和语言风格。后续写作、改写、润色时优先参考这里。

## 核心感觉

像开发者在记录自己真正做过的事情。

不是写总结报告，也不是把一个主题包装成“完整方法论”。

普通记录类文章更接近：

> 我为什么开始做这件事 → 实际怎么做 → 中间发现了什么 → 最后现在怎么用。

教程类文章可以直接从目标、结果或连接链路开始，不需要为了叙事完整补一段“为什么做”。

读起来应该自然、直接，有明确的个人判断，但不过度表达。

## 叙事原则

### 1. 真实动机存在时再写

如果确实有明确的个人动机，可以直接说最近在做什么、为什么开始折腾。

例如：

```text
最近高强度用网页 GPT 连接电脑 MCP 做开发。
```

但教程类文章如果主要目标是让读者复现流程，可以直接从：

```text
这篇先完成一个最小目标：让网页版 ChatGPT 通过 MCP 调用自己的电脑。
```

开始。

不要为了“像文章”而虚构背景、痛点、焦虑或故事。

避免：

```text
随着人工智能技术的快速发展，AI Agent 正在逐渐改变软件开发方式。
```

也避免为了制造冲突补：

```text
我一直被某个问题困扰，所以决定彻底重构整个工作流。
```

除非这件事真的发生过。

### 2. 按真实过程展开

文章顺序尽量接近事情真实发生的顺序。

记录类可以是：

```text
为什么做
↓
原来的问题
↓
先试了什么
↓
最后怎么做
↓
遇到的问题
↓
现在怎么用
```

教程类更适合：

```text
目标
↓
整体链路
↓
最小可用步骤
↓
验证
↓
进阶配置
↓
常见问题
```

不要为了结构完整强行加入“背景、挑战、方案、价值、展望”。

### 3. 技术方案先给上下文，再给实现

技术名词出现前，让读者知道它在链路里负责什么。

例如：

```text
网页版 GPT 访问不到电脑上的 127.0.0.1，所以需要一个公网可访问的 MCP 地址。
```

再讲 Tunnel、反向 SSH、Nginx、固定域名。

不需要先做一整段术语科普。

### 4. 实现逻辑只讲到能理解和复现

正文主要负责讲思路和关键步骤。

复杂实现、完整配置、完整 Prompt、项目结构，如果已经有 GitHub 仓库维护，可以：

- 只保留关键部分；
- 放到文章后半部分；
- 直接贴 GitHub 地址让读者看完整内容。

不要复制一整份项目文档到博客里。

### 5. 允许有个人判断

可以直接写：

```text
我没这么做。

这一步先不要急着做公网。

我比较喜欢这个设计。
```

不需要把每个判断包装成客观结论。

## 语言风格

### 直接

一句话能说清楚，就不要扩成一段。

优先：

```text
定时任务只负责再次唤醒 Agent。
```

避免：

```text
在实际搭建这套工作流的过程中，我逐渐意识到，定时任务本身其实并不是整个系统中最核心的部分。
```

### 口语化

用平时真的会说的话。

可以用：

- 挺有意思
- 没什么问题
- 先跑通
- 折腾
- 看一眼
- 没必要
- 就够了

不要故意写得“专业”“高级”。

### 短句

多用短句和短段落。

一个自然停顿就可以换段，不需要每段承担完整论证。

### 少形容词

尽量用事实代替形容。

不要反复使用：

- 强大
- 高效
- 优雅
- 革命性
- 极大提升
- 非常重要
- 值得注意的是

### 不煽情，不拔高

文章做完就做完。

避免结尾出现：

```text
这不仅仅是一次技术实践，更代表了 AI 时代全新的开发范式。
```

如果有总结，只总结现在怎么理解、怎么用，或者把读者指向完整仓库。

## 技术内容怎么写

### 代码和命令自己说话

能直接贴命令就不要再用三段话解释。

例如：

```bash
curl http://127.0.0.1:8765/healthz
```

正文只补一句这个命令是在验证什么。

### 不回避术语

Git、worktree、MCP、Nginx、OAuth、Task Board 可以直接写。

读者需要理解时顺手解释一句，不需要先做术语科普。

### 只保留关键细节

技术文章优先保留：

- 真正影响方案选择的细节；
- 实际踩过的坑；
- 最关键的命令或配置；
- 能帮助复现的最小信息。

删除：

- 和主线无关的完整参数解释；
- 重复验收步骤；
- 能直接去官方文档查到的大段说明；
- 为了显得完整而补的安全常识和背景知识；
- 为了制造故事性而补的虚构原因。

## 格式习惯

- 正文标题从 `##` 开始，文章 `title` 只放 frontmatter。
- 小节标题尽量直接，例如“先手动跑一条反向 SSH”，不要写“反向 SSH 隧道的构建与验证”。
- 能用普通段落就不用列表。
- 列表只在信息本身就是并列项时使用。
- 流程可以用简单的代码块和箭头表示。
- 不滥用粗体。
- 不为了排版制造过多小标题。

## AI 味重点检查

写完后重点删这些东西：

### 空泛开头

```text
随着……
在当今……
近年来……
```

### 虚构动机

如果文章真实过程里没有，就不要补：

```text
我一直有一个困扰……
真正让我决定做这件事的是……
用了几天以后我突然意识到……
```

教程可以没有故事开头。

### 结构提示词

```text
首先
其次
最后
综上所述
值得注意的是
需要强调的是
```

不是完全不能用，但不要靠它们组织全文。

### 重复总结

同一个观点说过一次就够了。

尤其检查文章后半部分有没有把前文再换一种方式讲一遍。

### 伪升华

删除：

```text
这不仅是……更是……
这标志着……
未来我们可以期待……
```

除非真的有新的信息。

### 过度解释

默认读者有基本理解能力。

不要每贴一段代码就再把代码逐行翻译一遍。

## 写完后的自检

提交前快速问自己：

1. 开头的信息是真实的吗？教程是否可以直接进入目标？
2. 文章顺序是不是接近真实过程或真实复现路径？
3. 有没有为了“完整”加入实际不需要的内容？
4. 有没有虚构动机、痛点或焦虑？
5. 同一个观点有没有讲两遍以上？
6. 技术细节是不是超过理解和复现所需的程度？
7. 有没有明显的 AI 套话、总结和升华？
8. 能不能再删 20% 而不损失关键信息？

如果可以删，就删。

## 一句话版本

**真实内容 + 直接短句 + 过程或复现路径驱动 + 技术细节够用就停 + 不虚构动机 + 少解释 + 少总结 + 不升华。**
