# 博客写作风格

这份文档用于约束博客文章的叙事和语言风格。后续写作、改写、润色时优先参考这里。

## 核心感觉

像开发者在记录自己真正做过的事情。

不是写教程，不是写总结报告，也不是把一个主题包装成“完整方法论”。

更接近：

> 我为什么开始做这件事 → 实际怎么做 → 中间发现了什么 → 最后现在怎么用。

读起来应该自然、直接，有明确的个人判断，但不过度表达。

## 叙事原则

### 1. 从真实动机开始

开头直接说最近在做什么、为什么开始折腾。

优先：

```text
最近高强度用网页 GPT 连接电脑 MCP 做开发。

之前用 Codex 的时候，我也折腾过任务板、定时任务这类东西。
```

避免：

```text
随着人工智能技术的快速发展，AI Agent 正在逐渐改变软件开发方式。
```

不要为了“像文章”而补背景。

### 2. 按真实思考过程展开

文章顺序尽量接近事情真实发生的顺序：

```text
为什么做
↓
原来的问题
↓
先试了什么
↓
最后怎么做
↓
遇到的问题
↓
现在怎么用
```

不要为了结构完整强行加入“背景、挑战、方案、价值、展望”。

### 3. 先讲为什么，再给实现

技术方案出现前，先让读者知道为什么需要它。

例如先说：

```text
网页版 GPT 访问不到我家里 Mac 的 127.0.0.1。
```

再讲反向 SSH、Nginx、固定域名。

不要一上来先解释一堆技术名词。

### 4. 实现逻辑只讲到能理解

正文主要负责讲思路和关键步骤。

复杂实现、完整配置、完整 Prompt、项目结构，如果不是文章主线，可以：

- 只保留关键部分；
- 放到文章后半部分；
- 直接贴 GitHub 地址让读者看完整内容。

不要把文章写成项目文档的复制品。

### 5. 允许有个人判断

可以直接写：

```text
我没这么做。

这一步先不要急着做公网。

我比较喜欢这个设计。
```

不需要把每个判断包装成客观结论。

## 语言风格

### 直接

一句话能说清楚，就不要扩成一段。

优先：

```text
定时任务不是最重要的。
```

避免：

```text
在实际搭建这套工作流的过程中，我逐渐意识到，定时任务本身其实并不是整个系统中最核心的部分。
```

### 口语化

用平时真的会说的话。

可以用：

- 挺有意思
- 没什么问题
- 先跑通
- 折腾
- 看一眼
- 没必要
- 就够了

不要故意写得“专业”“高级”。

### 短句

多用短句和短段落。

一个自然停顿就可以换段，不需要每段承担完整论证。

### 少形容词

尽量用事实代替形容。

不要反复使用：

- 强大
- 高效
- 优雅
- 革命性
- 极大提升
- 非常重要
- 值得注意的是

### 不煽情，不拔高

文章做完就做完。

避免结尾出现：

```text
这不仅仅是一次技术实践，更代表了 AI 时代全新的开发范式。
```

如果有总结，只总结自己现在怎么理解、怎么用。

## 技术内容怎么写

### 代码和命令自己说话

能直接贴命令就不要再用三段话解释。

例如：

```bash
curl http://127.0.0.1:8765/healthz
```

正文只补一句这个命令是在验证什么。

### 不回避术语

Git、worktree、MCP、Nginx、OAuth、Task Board 可以直接写。

读者需要理解时顺手解释一句，不需要先做术语科普。

### 只保留关键细节

技术文章优先保留：

- 真正影响方案选择的细节；
- 实际踩过的坑；
- 最关键的命令或配置；
- 能帮助复现的最小信息。

删除：

- 和主线无关的完整参数解释；
- 重复验收步骤；
- 能直接去官方文档查到的大段说明；
- 为了显得完整而补的安全常识和背景知识。

## 格式习惯

- 正文标题从 `##` 开始，文章 `title` 只放 frontmatter。
- 小节标题尽量直接，例如“先手动跑一条反向 SSH”，不要写“反向 SSH 隧道的构建与验证”。
- 能用普通段落就不用列表。
- 列表只在信息本身就是并列项时使用。
- 流程可以用简单的代码块和箭头表示。
- 不滥用粗体。
- 不为了排版制造过多小标题。

## AI 味重点检查

写完后重点删这些东西：

### 空泛开头

```text
随着……
在当今……
近年来……
```

### 结构提示词

```text
首先
其次
最后
综上所述
值得注意的是
需要强调的是
```

不是完全不能用，但不要靠它们组织全文。

### 重复总结

同一个观点说过一次就够了。

尤其检查文章后半部分有没有把前文再换一种方式讲一遍。

### 伪升华

删除：

```text
这不仅是……更是……
这标志着……
未来我们可以期待……
```

除非真的有新的信息。

### 过度解释

默认读者有基本理解能力。

不要每贴一段代码就再把代码逐行翻译一遍。

## 写完后的自检

提交前快速问自己：

1. 开头是不是直接进入我为什么做这件事？
2. 文章顺序是不是接近真实过程？
3. 有没有为了“完整”加入实际不需要的内容？
4. 同一个观点有没有讲两遍以上？
5. 技术细节是不是超过理解文章所需的程度？
6. 有没有明显的 AI 套话、总结和升华？
7. 能不能再删 20% 而不损失关键信息？

如果可以删，就删。

## 一句话版本

**真实经历 + 第一人称 + 过程驱动 + 直接短句 + 技术细节够用就停 + 少解释 + 少总结 + 不升华。**
