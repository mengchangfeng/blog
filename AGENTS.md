# 项目协作规则

- 本目录中的每次 Git 提交完成后，必须立即将当前分支推送到对应的远端分支。
- 推送失败时，应保留本地提交，并报告失败原因，不得假装推送成功。

## 博客文章格式

博客文章放在 `content/` 目录下，使用 Markdown 编写。

每篇文章必须以 frontmatter 开头，至少包含以下字段：

```md
---
title: 文章标题
date: YYYY-MM-DD
description: 一句话文章简介
tags: [标签1, 标签2, 标签3]
---

## 第一个正文小节

正文内容……

## 第二个正文小节

正文内容……
```

写作时遵循以下规则：

- 文章标题只写在 frontmatter 的 `title` 中。
- 正文中不要再写 `# 文章标题`，文章详情页会根据 `title` 自动渲染唯一的 `<h1>` 标题；重复写会导致页面显示两个标题。
- 正文一级章节从 `##` 开始，子章节依次使用 `###`、`####`。
- `description` 用于文章列表、文章页摘要和页面元信息，应简洁概括文章内容。
- `tags` 使用 Markdown 数组形式，例如 `[AgentDock, ChatGPT, MCP]`。
- `date` 使用 `YYYY-MM-DD` 格式。
- 文件名用于生成文章 URL slug，可以与标题不同；修改文件名会改变文章 URL，应谨慎操作。
